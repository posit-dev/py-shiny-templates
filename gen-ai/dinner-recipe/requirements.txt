shinyswatch
shiny @ git+https://github.com/posit-dev/py-shiny.git@main  # TODO: release - revert to PyPI `shiny>=1.7.0` once 1.7.0 is published (py-shiny#2364)
openai
dotenv
faicons
chatlas[anthropic]
# Temporary workaround so connect cloud will pick up latest version
httpcore>=1.0.8
